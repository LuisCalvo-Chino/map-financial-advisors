{
  "scripts": {
    "dev": "vite",
    "gmail-backend": "npm run dev --prefix backend",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "dompurify": "^3.3.3",
    "firebase": "^12.11.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.3.5"
  }
}
