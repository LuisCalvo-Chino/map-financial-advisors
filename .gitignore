# Dependencias y build
node_modules/
dist/

# Notas y documentación solo local (no subir a GitHub)
local-dev/

# Secretos y entorno local (nunca subir)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Logs y Firebase local
*.log
.firebase/
serviceAccount*.json
*-firebase-adminsdk-*.json

# Scripts Windows locales (no subir), excepto el asistente de push al repo
*.bat
!push-github-map.bat

# Sistema operativo
.DS_Store
Thumbs.db
.vercel
