# Dependencias y build
node_modules/
dist/

# Notas y documentación solo local (no subir a GitHub)
local-dev/

# Instrucciones / planes en la raíz del repo (solo local).
# README.md no se ignora: sigue siendo la ficha pública del proyecto en GitHub.
/PROJECT_OVERVIEW.md
/PLAN_*.md

# Secretos y entorno local (nunca subir)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Logs y Firebase local
*.log
.firebase/
serviceAccount*.json
*-firebase-adminsdk-*.json

# Scripts Windows (.bat) solo en tu PC; no versionar (evita exponer flujo de push en el repo público)
*.bat

# Sistema operativo
.DS_Store
Thumbs.db
.vercel
